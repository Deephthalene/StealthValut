# StealthVault – 놓친 부분 / 미구현 / 보완 필요 항목 총정리

> 대화·피드백·STEALTHVAULT_DEV_TASKS.md 종합 정리
> 2025-02-24 기준

---

## 1. 보안·Stealth 설계 (원래 계획 vs 현재)

### 1.1 header_encrypted (헤더 변조)

| 항목             | 설계                        | 현재        | 상태   |
| ---------------- | --------------------------- | ----------- | ------ |
| 원본 헤더 분리   | 1~8KB 잘라서 암호화         | 1~8KB 분리  | 구현됨 |
| DB 저장          | `header_encrypted` BLOB     | 저장        | 구현됨 |
| 물리 파일 앞부분 | 더미/랜덤 데이터로 덮어쓰기 | 랜덤 데이터 | 구현됨 |
| 복호화 시        | DB 헤더 + 본문 합쳐서 전달  | 지원        | 구현됨 |

- **의도**: 윈도우 탐색기·포렌식이 파일 종류(매직 넘버)를 알 수 없게 함
- **현재**: 전체 파일 AES-256-GCM만 사용 → "암호화된 파일"이라는 건 바로 알 수 있음

### 1.2 메타데이터 암호화

| 필드            | 설계 (STEALTHVAULT_DEV_TASKS) | 현재                | 상태   |
| --------------- | ----------------------------- | ------------------- | ------ |
| `original_name` | 암호화 저장                   | ENC:+base64 저장    | 구현됨 |
| `original_path` | 암호화 저장                   | ENC:+base64 저장    | 구현됨 |
| `created_at`    | 암호화 또는 난독화            | Unix timestamp 평문 | 미구현 |
| `mime_type`     | 암호화                        | NULL/평문           | 미구현 |

- **문제**: DB 덤프만 해도 "이건 파일 목록"이라고 바로 알 수 있음. `created_at` 숫자도 타임스탬프로 쉽게 해석됨.

### 1.3 확장자 위장 (Gemini 피드백)

| 설계/피드백                                 | 현재          | 상태   |
| ------------------------------------------- | ------------- | ------ |
| `.enc` 대신 `.dat`, `.bin` 또는 확장자 없음 | `{uuid}.dat`  | 적용됨 |
| "금고 느낌" 제거                            | `.dat`로 위장 | 적용됨 |

---

## 2. 파일 입고(Deposit) 관련

### 2.1 파일 이동 방식 (Gemini 피드백)

| 권장                                      | 현재                              | 상태   |
| ----------------------------------------- | --------------------------------- | ------ |
| 같은 드라이브: `fs::rename`으로 즉시 이동 | rename 실패 시 copy+secure_delete | 구현됨 |

- **효과**: 같은 드라이브에서는 rename이 더 빠르고 원자적

### 2.2 원본 삭제 방식 (STEALTHVAULT_DEV_TASKS 3.5)

| 설계                                  | 현재            | 상태   |
| ------------------------------------- | --------------- | ------ |
| 제로 필(Zero-fill) 삭제로 포렌식 방지 | `secure_delete` | 구현됨 |

- **의도**: 삭제 후에도 디스크에 원본 데이터 잔여 가능성 → 제로 필로 덮어쓰기

### 2.3 청크 단위 암호화 (STEALTHVAULT_DEV_TASKS 10.2)

| 설계                                | 현재                            | 상태      |
| ----------------------------------- | ------------------------------- | --------- |
| AES-256-GCM 청크 단위 (메모리 효율) | 전체 파일 한 번에 메모리에 로드 | 전체 로드 |

- **문제**: 대용량 파일 시 메모리 사용량 큼

---

## 3. 금고 저장소 형태 (STEALTHVAULT_DEV_TASKS 3.4)

| 설계                             | 현재                                  | 상태                     |
| -------------------------------- | ------------------------------------- | ------------------------ |
| `vault.data` 형태 단일 파일      | `vault.db` + `data/` 폴더에 개별 파일 | 폴더+파일 구조           |
| Sparse file로 실제 사용량만 점유 | 일반 파일                             | 미구현                   |
| 시스템 파일처럼 위장 저장        | AppData/Local/StealthVault 경로       | 경로는 위장, 형태는 아님 |

---

## 4. UI·성능 (5TB 시나리오 대비)

### 4.1 Virtual List / Lazy Loading (STEALTHVAULT_DEV_TASKS 3.7, 10.1)

| 설계                                | 현재                      | 상태        |
| ----------------------------------- | ------------------------- | ----------- |
| 수만 개 파일 렉 없이 목록 표시      | `files.map()` 전체 렌더링 | 전체 렌더링 |
| 페이징(Offset/Limit)                | 없음                      | 미구현      |
| Virtual List (화면에 보이는 부분만) | 없음                      | 미구현      |

- **참고**: 라우터에 `lazy` 로딩은 있으나, VaultPage 내부 목록은 아님

### 4.2 전용 뷰어·플레이어 (STEALTHVAULT_DEV_TASKS 4.1)

| 항목        | 설계                                | 현재                | 상태   |
| ----------- | ----------------------------------- | ------------------- | ------ |
| 사진/동영상 | 앱 내장 뷰어                        | 없음 (더블클릭 시?) | 미구현 |
| 스트리밍    | Range 청크 복호화 + custom_protocol | 없음                | 미구현 |
| EXE/게임    | 폴더 단위 가상 드라이브 마운트      | 없음                | 미구현 |
| 윈도우 흔적 | 최근 항목·Jump Lists 등 미기록      | -                   | 미구현 |

---

## 5. 스텔스·UX (Gemini / STEALTHVAULT_DEV_TASKS 4.2)

| 항목          | 설계/피드백                               | 현재 | 상태   |
| ------------- | ----------------------------------------- | ---- | ------ |
| 트레이 최소화 | 앱 닫아도 트레이에 숨기고 단축키로만 표시 | -    | 미구현 |
| 가상 드라이브 | 실행 중에만 마운트, 종료 시 사라짐        | 없음 | 미구현 |

---

## 6. 추가 보안 (STEALTHVAULT_DEV_TASKS 4.4)

| 항목                         | 설계                  | 현재                            | 상태      |
| ---------------------------- | --------------------- | ------------------------------- | --------- |
| 앱 종료 시 키 제로아웃       | 메모리에서 키 Zeroing | key_cache만 있음, 제로아웃 불명 | 확인 필요 |
| 최근 사용·미리보기 캐시 방지 | 윈도우 기록 방지      | 미구현                          | 미구현    |

---

## 7. 이미 구현된 것 (참고)

- Tauri + Rust + React 프로젝트 구조
- check_quota, 5GB 제한
- AES-256-GCM 파일 암호화/복호화
- SQLite 메타데이터 (평문)
- 파일 입고/출고 (이동 의미)
- 드래그앤드롭 (안쪽·바깥쪽)
- 썸네일 (암호화 전 생성, DB 저장, on-demand 보완)
- 비밀번호·복구키 기반 DEK/KEK
- AppData 경로 위장 (StealthVault 폴더)
- 구글 드라이브 스타일 UI, 폴더 트리

---

## 8. 우선 구현 제안 (보안·Stealth 우선)

1. **header_encrypted** 구현 (헤더 분리, 물리 파일 앞부분 더미 대체) ✅
2. **메타데이터 암호화** (original_name, original_path) ✅ (created_at은 인덱싱 때문에 미구현)
3. **확장자 위장** (`.enc` → `.dat` 등) ✅
4. **원본 제로 필 삭제** (포렌식 방지) ✅
5. **같은 드라이브 fs::rename** (업로드 속도) ✅
6. **Virtual List / 페이징** (대용량 목록 대비) – 미구현

이후: 전용 뷰어, 스트리밍, 페이크 모드, 트레이 숨김 등.
