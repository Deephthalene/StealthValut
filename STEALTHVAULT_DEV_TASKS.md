# StealthVault 개발 과제 정리

> Gemini·효현이 형님 대화에서 도출된 개발 아이템 및 할 일 목록
> (STT·단순 챗봇 등은 제외, StealthVault 핵심 기능 위주)

---

## 1. 프로젝트 개요

- **프로젝트명**: StealthVault (가칭)
- **개념**: 비밀번호로 잠기는 로컬 암호화 금고 (은행 금고처럼 넣고 빼기)
- **원칙**: 제로 지식(Zero-Knowledge) – 개발자도 복구/접근 불가
- **배포**: 서버 없음, 앱 배포만 (데이터는 사용자 PC에만 저장)

---

## 2. 기술 스택

| 구분       | 기술                 | 비고                                  |
| ---------- | -------------------- | ------------------------------------- |
| 프레임워크 | Tauri (Rust + React) | Electron 대비 경량·보안               |
| 백엔드     | Rust                 | 파일 시스템·암호화 제어               |
| 프론트엔드 | React                | 구글 드라이브 스타일 UI               |
| DB         | SQLite               | 메타데이터 저장 (암호화)              |
| 암호화     | AES-256-GCM          | 표준 암호화                           |
| 키 파생    | Argon2 / PBKDF2      | 비밀번호 → 암호화 키 (평문 저장 금지) |

---

## 3. MVP (1단계) 과제

### 3.1 프로젝트 초기 설정

- [x] Tauri + Rust + React 프로젝트 생성
- [x] 폴더/모듈 구조 잡기

### 3.2 비밀번호·키 관리

- [ ] 비밀번호를 평문으로 저장하지 않기
- [ ] KDF(Argon2/PBKDF2)로 비밀번호 → 암호화 키 생성
- [ ] 암호화 키는 메모리에만 존재, 작업 후 삭제
- [ ] **마스터 복구 키(Mnemonic)**: 설치 시 딱 한 번만 노출. 우리 서버에도 없다는 점을 유저에게 강력히 고지. AWS IAM 키처럼 한 번만 보여주는 컨셉

### 3.3 용량(Quota) 관리

- [x] 하드디스크 여유 공간 확인 (sysinfo)
- [x] 무료 사용자 5GB 소프트웨어 제한
- [x] `check_quota` 함수 구현
- [x] UI: "하드 공간 X GB 남음 / 금고 X GB / 5 GB" 표시

### 3.4 금고 컨테이너

- [ ] `vault.data` 형태의 암호화된 저장소 설계
- [ ] Sparse file로 실제 사용량만 디스크 점유
- [ ] `AppData` 등 시스템 경로에 위장 저장

### 3.5 파일 입고(Deposit)

- [ ] 드래그 앤 드롭으로 파일 추가
- [ ] AES-256-GCM 암호화 (청크 단위)
- [ ] **헤더 변조(Header Mangling)**: 매직 넘버(시그니처)를 무작위 바이트로 교체, 원본 헤더는 DB에 암호화 저장
- [ ] **Nonce 활용**: 같은 파일이라도 암호화할 때마다 결과가 달라지도록
- [ ] 파일명 → 해시값으로 변경 (`a1b2c3d4...` 등)
- [ ] 원본 파일 제로 필(Zero-fill) 삭제 (포렌식 방지)
- [ ] 메타데이터(원본 이름, 확장자, 썸네일 등) SQLite 저장 (암호화)

### 3.6 파일 출고(Withdraw)

- [ ] 앱 내 파일 목록에서 선택
- [ ] 복호화 후 사용자 지정 경로에 저장
- [ ] 원본 이름·확장자 복원

### 3.7 가상 탐색기 UI

- [ ] 넷플릭스/구글 드라이브 스타일 파일/폴더 목록
- [ ] **Virtual List**: 수만 개 파일 목록 렉 없이 (페이징 Offset/Limit, Lazy Loading)
- [ ] 썸네일은 DB에서 로드, 실제 데이터는 더블클릭 시에만 인메모리 복호화
- [ ] 비밀번호 입력 후 금고 열기
- [ ] 메인 대시보드 레이아웃

---

## 4. 고급(2단계) 과제

### 4.1 파일 미리보기·실행

- [ ] **사진/동영상**: 앱 내장 뷰어 (최근 항목·캐시 흔적 없음)
- [ ] **스트리밍 뷰어**: 5GB 영상도 버퍼링 없이 → **Tauri custom_protocol**로 암호화된 파일의 특정 구간(Range)만 청크 단위 복호화 후 로컬 스트리밍. 메모리 극소화
- [ ] **EXE(게임 등)**: 게임은 `.dll`, 데이터 폴더 등 의존성이 많으므로 **폴더 통째로 가상 드라이브(V:) 마운트** 후 그 안에서 실행. EXE 하나만 꺼내는 방식은 불가
- [ ] **APK**: 에뮬레이터 연동 브릿지
- [ ] **문서(PDF 등)**: 필요 시에만 임시 복호화 후 열람

### 4.2 스텔스 기능

- [ ] **가상 드라이브**: 실행 중에만 메모리에 마운트, 종료 시 사라짐
- [ ] **페이크 모드(Decoy)**: 앱을 메모장·계산기처럼 위장
- [ ] 숨겨진 제스처/단축키로만 금고 화면 진입
- [ ] 시스템 트레이에만 숨기고, 특정 단축키로만 표시

### 4.3 유료화(프로)

- [ ] **LicensingManager**: 라이선스 키 입력 시 로컬 DB의 `storage_limit` 컬럼 업데이트 (5GB → 무제한)
- [ ] 유료 결제 시 특정 알고리즘으로 생성된 오프라인 키 발급 (AWS IAM 키처럼 딱 한 번만 노출)
- [ ] 자폭: 비밀번호 5회 오입력 시 금고 데이터 암호화 폐쇄
- [ ] 멀티 금고 (업무용·취미용 등 여러 개)

### 4.4 추가 보안

- [ ] 앱 종료 시 메모리에서 키 제로아웃(Zeroing)
- [ ] 윈도우 최근 사용·미리보기 캐시 기록 방지

---

## 5. 커서에게 줄 첫 프롬프트 예시

```
Rust와 Tauri, React를 사용하여 'StealthVault'라는 로컬 파일 금고 앱을 만들 거야.

핵심 요구사항:
- 하드디스크의 특정 경로를 금고 저장소로 사용한다.
- 무료 사용자는 최대 5GB까지만 저장할 수 있도록 Rust 백엔드에서 용량을 체크해야 해.
- 파일 저장 시 AES-256-GCM으로 암호화하고, 파일 이름을 무작위 해시로 변경해서 저장해.
- 메타데이터(원본 이름 등)는 SQLite에 저장해줘.
- 사용자의 비밀번호는 절대 평문으로 저장하지 마. KDF로 키만 동적 생성.
- 주석은 최대한 간결하게 핵심 위주로 작성해줘.

먼저 Tauri 프로젝트 구조를 생성하고, check_quota 함수부터 작성해줘.
```

---

## 6. 비즈니스·배포

- [ ] 배포: 앱만 배포 (서버 없음)
- [ ] 결제: 라이선스 키 이메일 발송 (선택: 소규모 인증 API)
- [ ] 플랫폼: DLsite, BOOTH 등 (특히 일본 시장)
- [ ] 슬로건: "우리는 당신의 파일을 보지 못합니다. 당신의 하드디스크는 당신만의 것입니다."

---

## 7. 지원 파일 유형

| 유형     | 예시               | 처리 방식                           |
| -------- | ------------------ | ----------------------------------- |
| 이미지   | JPG, PNG 등        | 내장 뷰어                           |
| 영상     | MP4, MKV 등        | 내장 플레이어                       |
| 실행파일 | EXE, APK (야겜 등) | 임시 복호화 후 실행/에뮬레이터 연동 |
| 문서     | PDF, ZIP 등        | 필요 시 임시 복호화 후 열람         |

---

## 8. 배포 시 현실적 장애물 및 대응

> 개발 시작 전 대비책을 세워두면 나중에 고생을 덜 한다.

### 8.1 백신(Antivirus) 오진

| 문제 | 파일 암호화, 확장자 변경, 시스템 폴더 깊숙이 숨기기 → **랜섬웨어/멀웨어**와 기술적으로 유사. 윈도우 디펜더·알약 등이 차단/삭제할 수 있음 |
| ---- | ---------------------------------------------------------------------------------------------------------------------------------------- |
| 대응 | 설치/사용 가이드에 **"백신 예외 처리 해달라"**고 안내                                                                                    |

### 8.2 파일 실행 시 의존성 문제

| 문제 | 게임/프로그램은 `.exe` 하나가 아니라 `.dll`, 데이터 폴더 등이 묶여 있음. EXE만 꺼내서는 실행 불가                                                        |
| ---- | -------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 대응 | **폴더 통째로 가상 드라이브에 마운트**. 금고를 열면 V: 드라이브처럼 마운트되어, 그 안에서 게임을 설치된 그대로 실행. 아키텍처 설계 시 초기부터 고려 필요 |

### 8.3 데이터 손실 시 독박 위험

| 문제 | 비밀번호 분실·OS 재설치로 vault.data 삭제 시 → 수백 GB 영구 삭제. 사용자가 개발자 탓을 하게 됨                                                                                   |
| ---- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 대응 | **비밀번호 복구 프로세스** 필요. 금고 생성 시 **복구 키(Recovery Key)**를 발급하고, 사용자가 오프라인으로 안전한 곳에 보관하도록 유도. 비밀번호 분실 시 복구 키로 금고 열기 가능 |

### 8.4 유료 결제 우회(크랙)

| 문제 | 서버 없이 라이선스 관리 → 리버싱으로 `is_pro` 플래그를 true로 바꾸면 무료로 무제한 사용 가능                                            |
| ---- | --------------------------------------------------------------------------------------------------------------------------------------- |
| 대응 | - 완전 방어는 어려움. Rust 코드 **난독화(Obfuscation)** <br> - **초소형 인증 서버**로 실행 시마다 라이선스 키만 검증 (데이터는 안 받음) |

### 8.5 OS 흔적(윈도우 등)

| 문제 | 금고 안 영상 시청 시 '최근 본 항목', '플레이어 재생 목록', Jump Lists 등에 경로가 남음                                |
| ---- | --------------------------------------------------------------------------------------------------------------------- |
| 대응 | - 금고 내 파일은 **반드시 앱 내장 플레이어/뷰어**로만 열람 <br> - 실행 후 **임시 기록(Jump Lists 등) 청소** 로직 추가 |

> **가장 까다로운 도전과제**: `백신 오진 피하기`와 `가상 드라이브 마운트` 구현. 기술적으로 어렵지만, 해결 시 경쟁력과 수익성 확보에 크게 기여.

---

## 9. 구현 우선순위 제안

### 1단계: 기본 기능

- Tauri + Rust + React 프로젝트 생성
- `check_quota` (하드 여유 공간·5GB 제한)
- AES-256-GCM 파일 암호화/복호화 + 해시 파일명 저장
- SQLite 메타데이터 저장
- React UI: 비밀번호 입력, 파일 목록, 드래그 앤 드롭
- 입고(Deposit)·출고(Withdraw) 플로우
- 비밀번호 복구 프로세스 (복구 키 발급 및 저장 유도)

### 2단계: 스텔스·백신 대응

- 윈도우 디펜더 등에 걸리지 않는지 **실제 환경 테스트**
- 내장 사진/영상 뷰어 (외부 앱 호출 없음)
- OS 흔적(최근 항목, Jump Lists 등) 청소 로직

### 3단계: 고급 기능

- **가상 드라이브 마운트** (폴더 단위, 게임/프로그램 실행용)
- 임시 복호화 후 실행 + 종료 시 제로 필 삭제
- APK 에뮬레이터 연동 (선택)

---

## 10. 구체화 설계 (효현 대화 반영)

> "목록은 DB로 빠르게, 파일은 청크로 안전하게" – 5TB 대용량 시나리오 대응

### 10.1 데이터 레이어: "5TB도 0.1초 만에 띄우는 인덱싱"

- **SQLite = 고속 인덱스 사전**: 실제 파일은 조각나서 숨겨져 있으나, 앱 실행 시 암호화된 `vault.db`만 읽음
- **스키마**: 파일 경로, 해시, 썸네일(Base64 또는 바이너리), 메타데이터
- **프론트**: 파일 1만 개+ 렉 없음 → **페이징(Offset/Limit)** + **Lazy Loading** 필수

### 10.2 암호화: "바이트 난도질"

- **헤더 변조**: 파일 시그니처(매직 넘버)를 암호화된 별도 공간으로 옮기고, 그 자리에 무작위 데이터 채워 윈도우가 형식 추측 불가
- **Nonce 활용**: 같은 파일이라도 암호화할 때마다 결과 상이
- **청크 단위**: AES-256-GCM으로 청크 단위 암호화 (메모리 효율)

### 10.3 스트리밍 뷰어

- **custom_protocol**: Tauri 내부 로컬 서버에서 암호화 파일의 **Range**만 복호화 후 스트리밍
- 5GB 영상도 버퍼링·메모리 점유 극소화

### 10.4 SQLite 스키마 예시 (커서 지시용)

```sql
-- 파일 메타데이터 (암호화된 vault.db)
CREATE TABLE files (
  id TEXT PRIMARY KEY,
  original_name TEXT,
  original_path TEXT,
  hash_name TEXT,
  mime_type TEXT,
  size_bytes INTEGER,
  thumbnail_blob BLOB,
  header_encrypted BLOB,
  created_at INTEGER
);

CREATE TABLE vault_config (
  storage_limit INTEGER DEFAULT 5368709120
);
```

### 10.5 업그레이드 프롬프트 (커서용)

```
StealthVault의 핵심 엔진을 설계해줘. 핵심은 '성능'과 '흔적 제거'야.

백엔드(Rust):
- 5TB+ 대용량 목록 → SQLite 인덱싱
- 파일 추가 시 헤더 바이트 난독화 + AES-256-GCM 청크 단위 암호화
- Nonce 적극 활용

프론트엔드(React):
- 넷플릭스 스타일 UI, Virtual List로 수만 개 파일 렉 없이
- 썸네일 DB에서 로드, 실제 데이터는 더블클릭 시에만 인메모리 복호화

안티 포렌식:
- 모든 작업 앱 내 전용 뷰어
- 윈도우 '최근 항목' 절대 미기록

수익 모델:
- 기본 5GB 제한, 오프라인 라이선스 키로 해제 (LicensingManager)
- 주석은 간결하게
```
